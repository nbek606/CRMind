import { AuthWidget } from "@/widgets/auth";

export const LoginPage = () => {
    return (
        <div className="login__page" style={{width:'100%'}}>
            <AuthWidget />
        </div>
    )
}